# Local development overrides — NOT used on the server (not committed if you add it to .gitignore,
# but safe to commit since it only adds convenience port bindings).
services:
  postgres:
    ports:
      - "5432:5432"

  mercure:
    ports:
      - "3000:80"
